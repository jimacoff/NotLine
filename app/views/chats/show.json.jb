json = {
  id: @chat.id,
  title: @chat.title,
  current_user: render(partial: 'users/user', locals: {user: current_user}),
  partner: render(partial: 'users/user', locals: {user: @partner}),
  messages: render(partial: 'messages/message', collection: @chat.messages),
}
